-- JSON schemas for better JSON editing
return {
  "b0o/schemastore.nvim",
  lazy = true,
  config = function()
    -- No additional configuration needed
    -- Used by jsonls in nvim-lspconfig
  end,
}
